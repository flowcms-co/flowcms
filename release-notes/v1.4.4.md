## v1.4.4: correct install prompt name

A follow-up to v1.4.3. The in-app "Add to Home Screen" prompt still showed the wrong name.

- **Install prompt now says "FlowCMS".** The bottom sheet, the settings card, and the mobile
  banner were hardcoded to "Flow Admin". They now read "FlowCMS", matching the PWA manifest and
  the home-screen app name set in v1.4.3.

Studio-only, copy change; no behavior or API changes.
