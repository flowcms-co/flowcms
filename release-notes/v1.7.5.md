# v1.7.5: telemetry and housekeeping

Internal improvements to the install heartbeat: the vendor-reported
owner profile is richer, and telemetry configuration is now read at
send time rather than process start, which makes overrides like
FLOWCMS_ADMIN_URL reliable in every environment.

## Notes

Studio and API. No migrations, no config changes, no new environment
variables.
