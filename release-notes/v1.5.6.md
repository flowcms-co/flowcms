# v1.5.6: complete the v2 avatar pack

Fills the last empty slot in the v2 avatar pack. Character 6 (a woman with
shoulder-length wavy dark hair in a soft lilac blouse) now has finished
watercolor art, so all fourteen characters render from the v2 pack instead of
falling back to the classic piece for that slot.

- **Character 6 avatar.** Matches the rest of the pack: front-facing on a light
  pastel background, hand-painted in the same storybook style.

Studio asset only; no code, no new endpoints, no schema changes.
