## Fixes and improvements

- **Accurate seat usage.** The Plan page now shows seats used across your whole install (the same number the invite gate enforces), so "X of Y used" always matches reality. Removing seats is correctly blocked when it would drop below your current member count.
- Buying a seat takes effect immediately: the new allotment is recognized right away, so the invite that prompted it goes through without a wait.
