## Fixes and improvements

- **Crisp app icon everywhere.** The Flow CMS mark now ships as a proper multi-resolution
  `favicon.ico` plus an Apple touch icon, so the tab icon and bookmark render correctly in
  Safari (and every other browser), not just the SVG-only browsers.
